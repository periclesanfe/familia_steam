# Desenvolvimento local.
#   Só o banco (dev com `pnpm dev`):   docker compose up -d db
#   App completo em container:          docker compose --profile app up --build
name: familia-steam

# 14 SEG-08: o app usa app_rw; as migrações usam app_owner (criados por docker/initdb).
x-db-url: &db-url postgresql://app_rw:app_rw@db:5432/consorcio
x-migrate-url: &migrate-url postgresql://app_owner:app_owner@db:5432/consorcio

services:
  db:
    image: postgres:17-alpine
    # Ajuste para máquina pequena: poucas conexões (pool da app = 5).
    command: postgres -c max_connections=30 -c shared_buffers=64MB
    environment:
      POSTGRES_USER: consorcio
      POSTGRES_PASSWORD: consorcio
      POSTGRES_DB: consorcio
    ports:
      # 5433 no host para não colidir com outros Postgres locais (ajustável por DB_PORTA).
      - '${DB_PORTA:-5433}:5432'
    volumes:
      - db:/var/lib/postgresql/data
      - ./docker/initdb:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U consorcio -d consorcio']
      interval: 5s
      timeout: 3s
      retries: 10

  migrate:
    profiles: [app]
    build: { context: ., target: migrate }
    environment:
      MIGRATE_DATABASE_URL: *migrate-url
    depends_on:
      db: { condition: service_healthy }

  app:
    profiles: [app]
    build: { context: ., target: runner }
    init: true
    environment:
      DATABASE_URL: *db-url
      APP_URL: http://localhost:3100
    ports:
      - '${APP_PORTA:-3100}:3000'
    depends_on:
      migrate: { condition: service_completed_successfully }

volumes:
  db:
